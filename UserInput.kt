fun main(args : Array<String>) {

    println("Enter your name: ") // prompting the user
    var name = readLine() // using "readLine()" function to store the input from the user

    println("Hi $name") // printing the name of the user from the "name" variable
}